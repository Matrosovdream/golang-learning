services:
  app:
    build: .
    environment:
      APP_PORT: ${APP_PORT}
      WORKER_COUNT: ${WORKER_COUNT}
      DEFAULT_TIMEOUT_MS: ${DEFAULT_TIMEOUT_MS}
      MAX_URLS: ${MAX_URLS}
    ports:
      - "${APP_PORT}:${APP_PORT}"
