apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus            # dashboards reference this uid
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
