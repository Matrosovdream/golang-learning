# Progress

> Update this file as you finish each step. Claude will read it to know where to pick up.

## Current step

**→ 11 — Interfaces** (next up)

## Status by step

| #  | Step                                  | Status      | Notes |
|----|---------------------------------------|-------------|-------|
| 01 | Introduction to Go                    | done        | covered philosophy, why the boilerplate (package main/import/func main) |
| 02 | Environment Setup                     | done        | Go 1.26.3; module `practice/` = github.com/Matrosovdream/golang-learning/practice; per-lesson subfolders |
| 03 | First Program & Basic Syntax          | done        | 4 var forms, zero value, Println vs Printf, iota; saw unused import/var errors |
| 04 | Variables, Types & Constants          | done        | basic types & zero values, no implicit conversion (T(v)), untyped vs typed consts, iota enums, named types (Celsius/Fahrenheit) |
| 05 | Control Flow                          | done        | if-init + early return, all 3 for forms + range (incl. `range 5`), tagless switch (FizzBuzz), labeled break, defer LIFO + args-evaluated-immediately gotcha |
| 06 | Functions                             | done        | multi-return (T, error), variadic + slice spread, closures w/ independent state, funcs as values, defer+recover→named err, Go 1.22+ loop-var capture (prints 0 1 2) |
| 07 | Arrays, Slices & Maps                 | done        | slice len/cap + growth (doubling), aliasing trap, copy detaches, remove-by-index, word-freq map, comma-ok, nil-map panic+recover, set via map[T]struct{}; `go vet` catches Printf verb/arg mismatch |
| 08 | Strings, Runes, Bytes & Formatting    | done        | len=bytes vs []rune=chars, range decodes UTF-8 (index jumps), strconv Atoi/Itoa, string(rune(65))="A" vs Itoa="65", strings.Fields/Join, += vs strings.Builder (O(n²) vs O(n)), %v/%+v/%#v/%q/%.2f verbs. NOTE: used "hello" not "héllo" so multibyte demo was muted |
| 09 | Structs                               | done        | base: keyed/zero/&literal, value-copy vs pointer mutation, embedding (promoted field+method), json tags+omitempty, anon struct, comparability (slice field breaks ==). Extended drills (09-structs/02-07): New() constructor+validation, nested named field, ⚠️slice range-copy gotcha (mutate via items[i]), ⚠️map values non-addressable (copy-store or map[K]*V), embedding shadowing + interface satisfaction, JSON Marshal/Unmarshal + json:"-" + nested |
| 10 | Pointers & Methods                    | done        | base: &/*, value vs pointer receiver, auto-address/deref, nil-receiver guard, Celsius Stringer, linked-list Sum. Drills: method sets T vs *T (interface satisfaction), map-elem-not-addressable vs slice-elem-addressable, new(T). Deep pointer practice (4 progs): **int/swap, return-ptr-to-local=escape analysis, range copy vs index vs []*T, map[K]*V, *[N]T, optional *bool/*int JSON fields, **Node sorted insert |
| 11 | Interfaces                            | in progress | examples library: `examples/11-interfaces/README.md` (25 graded examples easy→hard, each verified). Per-lesson tracker: `examples/11-interfaces/PROGRESS.md`. Working through them now. |
| 12 | Errors & Error Handling               | not started |       |
| 13 | Goroutines                            | not started |       |
| 14 | Channels                              | not started |       |
| 15 | Sync, Context & Patterns              | not started | examples library ready: `examples/15-sync-context/` (40 graded examples, all run-verified incl. -race) |
| 16 | Packages & Modules                    | not started |       |
| 17 | Generics                              | not started |       |
| 18 | Testing & Benchmarking                | not started |       |
| 19 | Standard Library Tour for Backend     | not started |       |
| 20 | HTTP Server Fundamentals              | not started |       |
| 21 | Building a JSON REST API              | not started |       |
| 22 | Persistence with database/sql         | not started |       |
| 23 | Config, Logging & Observability       | not started |       |
| 24 | Idiomatic Go & Effective Go           | not started |       |
| 25 | Project Layout & Clean Architecture   | not started |       |
| 26 | Capstone Project: REST API Service    | not started |       |
| 27 | gRPC & Microservices (logging/metrics) | not started | lesson doc + examples library ready: `27-grpc-microservices.md` + `examples/27-grpc-microservices/` (24 graded examples easy→hard, all verified). Companion example-projects trio built & verified: `grpc-echo-beginner`, `grpc-orders-intermediate`, `grpc-observability-hard`. |
| 28 | Design Patterns I: Creational          | not started | lesson doc + examples ready: `28-patterns-creational.md` + `examples/28-patterns-creational/` (17 graded examples easy→hard, all run-verified). |
| 29 | Design Patterns II: Structural         | not started | lesson doc + examples ready: `29-patterns-structural.md` + `examples/29-patterns-structural/` (17 graded examples easy→hard, all run-verified). |
| 30 | Design Patterns III: Behavioral        | not started | lesson doc + examples ready: `30-patterns-behavioral.md` + `examples/30-patterns-behavioral/` (16 graded examples easy→hard, all run-verified incl. range-over-func 1.23+). |
| 31 | DDD (tactical)                         | not started | lesson doc + examples ready: `31-ddd-tactical.md` + `examples/31-ddd-tactical/` (15 graded examples easy→hard, all run-verified, stdlib-only). |
| 32 | Hexagonal / Ports & Adapters           | not started | lesson doc + examples ready: `32-hexagonal-ports-adapters.md` + `examples/32-hexagonal-ports-adapters/` (15 graded examples easy→hard, all run-verified, stdlib incl. net/http+httptest driving adapters). |
| 33 | Dependency Injection & Wiring          | not started | lesson doc + examples ready: `33-dependency-injection.md` + `examples/33-dependency-injection/` (15 graded examples easy→hard, all run-verified; wire/fx shown as stdlib stand-ins). |
| 34 | Event-Driven Architecture & Outbox     | not started | lesson doc + examples ready: `34-event-driven-outbox.md` + `examples/34-event-driven-outbox/` (15 graded examples easy→hard, all run-verified; in-mem broker/DB/outbox stand-ins). |
| 35 | Sagas & Distributed Transactions       | not started | lesson doc + examples ready: `35-sagas-distributed-transactions.md` + `examples/35-sagas-distributed-transactions/` (15 graded examples easy→hard, all run-verified; in-mem orchestrator/services). |
| 36 | Resilience Patterns                    | not started | lesson doc + examples ready: `36-resilience-patterns.md` + `examples/36-resilience-patterns/` (15 graded examples easy→hard, all run-verified, deterministic; seeded jitter). |
| 37 | CQRS & Event Sourcing                  | not started | lesson doc + examples ready: `37-cqrs-event-sourcing.md` + `examples/37-cqrs-event-sourcing/` (15 graded examples easy→hard, all run-verified; in-mem event store/projections). |
| 38 | Caching Patterns                       | not started | lesson doc + examples ready: `38-caching-patterns.md` + `examples/38-caching-patterns/` (15 graded examples easy→hard, all run-verified; TTL via injectable clock, LRU, mini singleflight [deterministic under -race]). |
| 39 | Observability: Distributed Tracing     | not started | lesson doc + examples ready: `39-observability-tracing.md` + `examples/39-observability-tracing/` (15 graded examples easy→hard, all run-verified; mini in-mem tracer, counter IDs + injectable clock = deterministic). |
| 40 | Testing Architecture                   | not started | lesson doc + examples ready: `40-testing-architecture.md` + `examples/40-testing-architecture/` (15 graded examples easy→hard, all run-verified; fakes/spies/mocks, injected clock, httptest, golden-file via os.MkdirTemp). |
| 41 | API Design & Evolution                 | not started | lesson doc + examples ready: `41-api-design-evolution.md` + `examples/41-api-design-evolution/` (15 graded examples easy→hard, all run-verified; net/http+httptest, cursor pagination, idempotency-key, problem+json, ETag/304). |

**Status legend:** `not started` · `in progress` · `done`

## Log

<!-- Append a short line each session, newest at the bottom. Example:
- 2026-05-28 — finished step 01, took notes on Go vs other languages
-->

- 2026-05-28 — learning plan created (26 steps, backend/REST-API focus). Start with step 01.
- 2026-05-29 — finished steps 01 & 02. Set up `practice/` Go module (per-lesson subfolders), verified `go run`/`fmt`/`vet`. Next: step 03.
- 2026-05-29 — finished steps 03 & 04 (first program, syntax, types/constants/iota). Saw unused-import/var and mismatched-types compile errors on purpose. Next: step 05.
- 2026-06-01 — finished step 05 (control flow): if-init, for forms + range, tagless switch FizzBuzz, labeled break, defer LIFO/arg-eval. Note: module root is `practice/`, so run from inside it (`cd practice && go run ./05-control-flow`). Next: step 06.
- 2026-06-01 — finished step 06 (functions): multi-return/(T,error), variadic + spread, closures, funcs as values, defer+recover→named err, loop-var capture. Next: step 07.
- 2026-06-02 — finished step 07 (slices & maps): cap growth/doubling, aliasing trap, copy, remove-by-index, word-freq map + comma-ok, nil-map panic+recover, set via map[T]struct{}. Learned `go vet` catches Printf verb/arg mismatches. Next: step 08.
- 2026-06-02 — finished step 08 (strings): bytes-vs-runes, range/UTF-8, strconv, string(rune(n)) vs Itoa, strings.Builder, fmt verbs. Moved fast — used ASCII so the multibyte len split was muted; reinforced go vet for Printf. Next: step 09.
- 2026-06-02 — finished step 09 (structs) + 6 extended drills (09-structs/02-07): constructors/validation, nested structs, slice range-copy gotcha, map non-addressability, advanced embedding (shadowing + interface), JSON round-trip. Going deeper per request. Next: step 10.
- 2026-06-03 — finished step 10 (pointers & methods): base lesson (&/*, value vs pointer receiver, nil receiver, Celsius Stringer, linked-list Sum) + drills (method sets T vs *T, map vs slice addressability, new) + extra deep pointer practice in 4 progs under 10-pointers-methods/{pointers-basics,pointers-funcs,pointers-collections,pointers-patterns}: **int/swap, return-ptr-to-local (escape analysis), range-copy vs index vs []*T, map[K]*V, *[N]T, optional *bool/*int JSON fields, **Node sorted insert. Next: step 11 (interfaces).
- 2026-06-10 — examples library: finished 09-structs (all 26 — wrapped up the JSON tag trio, NewX constructor, empty struct set). Next in the examples track: 10-pointers-methods (26 examples, none ticked).
- 2026-06-05 — started step 11 (interfaces). Began the lesson (Shape/Stringer/type-switch/typed-nil) directly in `practice/11-interfaces`, then created a NEW examples library: `learning-plan/examples/<lesson>/README.md`. Built `11-interfaces/README.md` with 25 graded examples easy→hard (implicit satisfaction, polymorphism, (type,value) pair, Stringer, any, assertions, type switch, method sets, composition, error iface, sort.Slice/sort.Interface, io.Writer/MultiWriter, iface-to-iface assertion, strategy map, accept-iface/return-struct, typed-nil trap, uncomparable panic, optional ifaces, decorator/middleware, recursive any walker, DI test fake, capstone plugin system). Each example verified (gofmt/vet/run); workflow used to author+verify. Process: I dictate code blocks, user retypes & runs. "add more examples" → append starting at #26. Next: user works through the examples.
- 2026-06-11 — examples library: finished 10-pointers-methods (all 24 — medium 13–16: slice-elem pointers, range copy vs index, []*T, map[K]*V; hard 17–24: method sets, iface satisfaction *T, map non-addressability, **T, nil receivers, pointer ==, &T{} vs new, *Stack Push). Started 11-interfaces examples: #1 (implicit satisfaction) done. Next: 11-interfaces #2 (polymorphism).
- 2026-06-11 — built examples library for step 15: `examples/15-sync-context/` (40 examples: easy 1–10 race detector/mutex/RWMutex/Once + context intro; medium 11–28 mutex-vs-channel, TryLock, atomic.Pointer, Pool, sync.Map, full context API; hard 29–40 ctx worker pool/pipeline, mini errgroup, Cond, graceful shutdown, double-check, token bucket, heartbeats, singleflight, ctx semaphore, capstone scheduler). Every example compiled, vetted, run & output-diffed (incl. -race). Designed to NOT overlap 13-goroutines/14-channels.
- 2026-06-12 — example-projects track: rebuilding `task-manager-beginner` dictation-style into `projects/task-manager/` (own module `taskmanager`; reference copy untouched). All 8 Go files typed: domain (enums+errors+repo iface), config, GORM repository (model/domain split, whitelisted ORDER BY, map Updates), service (validation+defaults), handler (DTOs, errors.As/Is→400/404), router (Go 1.22 stdlib routing + middleware), main (wiring, connect-retry, graceful shutdown). Gotchas hit live: `go mod tidy` prunes unused requires; trailing commas in composite literals; interface satisfaction only checked at wiring (GetById vs GetByID). ▶ Resume: rename GetById→GetByID in the rebuild's task_repository.go:50, rebuild, then Docker infra + run & verify (tracked in task-manager-beginner/progress.md).
- 2026-06-14 — ✅ **FINISHED** new example project `poll-hub-microservices-beginner` (authored reference copy + rebuilt at `projects/poll-hub/`). First microservices project: 3 flat services (poll :8081 / vote :8082 / stats :8083) sharing ONE Postgres + ONE root docker-compose, raw SQL via pgx. Deliberately NOT clean-arch — flat `package main` per service (main/handlers/store), one shared `go.mod`, shared `internal/{env,httpx,postgres}`. Concepts: service-to-service HTTP (vote calls poll's `GET /polls/{id}` before inserting → 404/502/409/400 ladder), DB constraints AS business rules (`UNIQUE (poll_id, voter)` → 23505 → 409; composite FK `(option_id, poll_id)` makes cross-poll votes unrepresentable), Go 1.22 stdlib routing (`mux.HandleFunc("POST /polls/{id}/close")` + `r.PathValue`), one parameterized Dockerfile (`ARG SERVICE`), schema as mounted `db/init.sql` (no migration code). User typed scaffold + shared toolkit + db/init.sql + poll-service by hand; vote/stats + init.sql were copied from reference while fixing Docker startup. Live gotchas: bind-mount of a MISSING file → Docker creates an empty *directory* → Postgres "Is a directory" crash; `docker compose up --build` builds ALL services → "no Go files in /src/services/vote" until they exist (use `up --build db poll` for a subset). Verified end-to-end: full curl pass green. Added to example-projects README as project #3 (track now 8 projects). Next: continue interfaces examples, or pick the next example-project.
- 2026-06-17 — ✅ **FINISHED (dictation walkthrough)** example project `auth-service-intermediate` — rebuilt at `projects/auth-service/` (module `authservice`; reference copy untouched & verified `go build ./...` + `go vet ./...` clean before dictating). Project #5 in the track; **clean-arch + raw SQL via sqlx** (a different flavour from project 1's native pgx) over the pgx stdlib driver. All 15 files typed by hand across 5 stages: domain (User + UserRepository iface + ErrEmailTaken/ErrUserNotFound/ErrInvalidCredentials + ValidationError), config (JWT_SECRET/TOKEN_TTL), `auth` pkg (JWTManager Generate/Parse, HS256, **rejects non-HMAC alg** = alg-confusion defense), sqlx repository (userRow db-tags, `INSERT…RETURNING` via QueryRowxContext, 23505→ErrEmailTaken, sql.ErrNoRows→ErrUserNotFound), service (bcrypt hash/compare, email normalise, **uniform ErrInvalidCredentials = no user enumeration**, self-declared `TokenIssuer` iface keeps JWT out of the core), middleware (RequestID/Logger/Recover + `Auth` gate taking a `parse func` so it never imports `auth`, ctxKey type, Chain), handler (DTOs with **no password_hash field**, errors→HTTP 400/409/401/404/500), router (Go 1.22 method routing, only /me wrapped in Auth), main (registers pgx stdlib driver, connect-with-retry, graceful shutdown). Infra: 001_init.sql (UNIQUE email), multi-stage Dockerfile non-root, compose with healthcheck + initdb mount, .env, .dockerignore. Concepts taught: hashing, stateless JWT, auth middleware + context identity, keeping a 3rd-party concern out of the core via an interface, security hygiene (no hash in responses, uniform login errors). Also fixed the stale ordinal in this project's README ("fourth"→"fifth", since poll-hub's insertion bumped it). ▶ Resume: in `projects/auth-service/` run `go mod tidy` then `docker compose up --build` and the curl pass to tick the 7 Run & verify boxes (the only ones still open). Stale README ordinals remain in blog-api/order-service/shop/event-shop (off-by-one from poll-hub insertion) — offered, not yet fixed.
- 2026-06-22 — 🚧 **IN PROGRESS (dictation walkthrough)** example project `watch-hub-intermediate` — rebuilding at `projects/watch-hub/` (module `watchhub`; reference copy untouched). Project #6 in the track, the **intermediate-concurrency** one: a background uptime monitor (grown-up sibling of `ping-hub-beginner`, which probes synchronously). One external dep: `golang.org/x/sync` (errgroup); storage in-memory by design. Typed by hand so far (top-down by layer, `go build ./...` green after each): scaffold + `go.mod` (`go get golang.org/x/sync@v0.18.0`); domain/monitor.go (Status enum pending/up/down/error, Monitor/MonitorView/CheckResult, MonitorRepository iface, ErrNotFound + ValidationError); config/config.go (concurrency knobs MAX_CONCURRENT_CHECKS/RATE_PER_SEC/RATE_BURST/MAX_RETRIES/HISTORY_SIZE/MAX_MONITORS/DEFAULT_TIMEOUT_MS); repository/memory/monitor_store.go (★ **two-level locking** — one RWMutex for the map + a per-entry RWMutex; List() releases the map lock before locking entries; history **ring** capped at histSize; view() deep-copies + reverses to newest-first); service/limiter.go (**token bucket** = ticker dripping into a buffered channel, starts full, non-blocking refill, nil-safe Wait(ctx)); service/checker.go (★ **channel semaphore** cap=MaxConcurrent acquired via select-vs-ctx + **rate limit** + **retry/backoff** — only transport errors retry, 4xx/5xx is a real answer; per-attempt context.WithTimeout; atomics inFlight/totalChecks; sleepCtx cancellable mid-sleep); service/events.go (**SSE pub/sub EventHub** = actor pattern, one goroutine owns the subscribers map so NO mutex; non-blocking broadcast drops slow clients); service/scheduler.go (★ **goroutine-per-monitor** ticker loop, **context tree** root→per-monitor so delete cancels one / Stop cancels all, broadcasts only on status *change*, **WaitGroup drain** in Stop, Stats() via atomics + runtime.NumGoroutine); handler/response.go (writeJSON/writeError); handler/monitor_handler.go (CRUD + CheckNow + **errgroup batch** `g.SetLimit(n)` as the 2nd way to bound a fan-out, DTOs, writeServiceError→400/404). ▶ **Resume: next file to type is `internal/handler/events_handler.go`** (SSE Stream: Flusher assertion, `: connected` open, select over event/heartbeat-15s/ctx.Done), then `internal/router/router.go` (routes + logging/recover middleware) + `cmd/api/main.go` (wiring + graceful drain: Shutdown→sched.Stop→hub/limiter Close), then infra (Dockerfile/docker-compose/.env/.dockerignore), then run & verify (`go build`/`go vet`, `-race` smoke test). Per-file run-checklist lives in `example-projects/watch-hub-intermediate/progress.md`.
- 2026-07-06 — ✅ **FINISHED** new **lesson 27 — gRPC & Microservices (logging & metrics)** + its companion example-project trio. (1) Lesson doc `27-grpc-microservices.md` (contract-first protobuf, server/client, 3 streaming modes, status codes, metadata, interceptors, request-id propagation, slog + Prometheus + Grafana, db-per-service, API gateway). (2) Examples library `examples/27-grpc-microservices/` — **24 graded examples easy→hard** across README/1-easy/2-medium/3-hard/PROGRESS; all authored + run-verified in a scratch module (shared `greet.proto` + generated stubs; most self-contained via **bufconn** so one `go run .` shows a full client+server round trip). Covers: proto/codegen, wire format, deadlines, status codes, real-TCP Unavailable, field presence, all 3 streaming kinds, metadata, unary/stream/client interceptors, chaining, **request-id propagation across a hop**, recovery, slog JSON, **Prometheus interceptor + /metrics**, health, reflection, retry/backoff, capstone. (3) Example-projects trio (own modules, generated `*.pb.go` committed, grpc v1.81.1 / protobuf v1.36.11 / prometheus client v1.23.2 / pgx v5.10.0): **grpc-echo-beginner** (gateway→echo, slog + request-id via metadata, code→HTTP; smoke-tested: id in both logs, 503 when echo down), **grpc-orders-intermediate** (gateway + catalog + orders, in-memory, clean-arch, request-id across all 3, per-service Prometheus `/metrics`; smoke-tested: checkout fan-out total 12500, 404/409 mapping, id in 3 logs, counters), **grpc-observability-hard** (same mesh + **Postgres per service** w/ `SELECT … FOR UPDATE`, gRPC **health**, **Prometheus + Grafana** provisioned dashboard [rate/p95/error-ratio/in-flight]; smoke-tested with real Postgres incl. a **5-way concurrent oversell test → 2×201 / 3×409, stock floored at 0**). All three: `go build`/`vet`/`gofmt` clean, configs (dashboard JSON, prometheus.yml, grafana YAML, compose) validated. example-projects/README (now 11 projects) + this file updated. These are **reference copies** (authored by me); user rebuilds into `projects/` dictation-style if desired. Next: user works through the examples, or rebuilds a project.
- 2026-07-06 — ✅ **NEW Design Patterns series (lesson docs)** — blended GoF + idiomatic-Go, split into three Part-8 lessons because one would be far too long: **28-patterns-creational.md** (useful zero value, `NewT`, functional options, fluent builder, factory + `init()` registry, abstract factory, `sync.Once` singleton vs DI, `sync.Pool` object pool, deep `Clone()` prototype), **29-patterns-structural.md** (embedding=delegation-not-inheritance, interface-embedding decorator, small consumer-defined interfaces, `HandlerFunc` adapter + struct adapter, middleware `Chain`, facade=service layer, caching/access proxy, composite file tree, bridge=DI, flyweight/interning), **30-patterns-behavioral.md** (strategy/command as function values + `slices.SortFunc`, the **Template-Method embedding trap** + inject-a-`Step`-interface fix, `Do`/`Undo` command history, observer callback-slice vs channel, transition-table state machine, **range-over-func iterators** `iter.Seq`/`iter.Seq2` [Go 1.23+] + the leaky channel iterator contrast, chain-of-responsibility, type-switch visitor over a sealed `Expr`). Each lesson uses the standard Goals/Concepts/Exercises/Best-Practices/Checklist/Resources shape and cross-links the trio. Explicitly does NOT re-teach concurrency patterns (those stay in lesson 15) or general style (lesson 24). README Part 8 + this table updated. **Examples libraries NOT built yet** — say "add examples for 28/29/30" to get the graded easy→medium→hard trio like lesson 27. Key snippets (functional options, middleware chain, embedding-trap, range-over-func iterator, type-switch eval) compile-checked in a scratch module. Next: user reads the lessons, or asks for the examples library.
- 2026-07-07 — ✅ **NEW Part 9 — Architecture & System Design (11 lesson docs, 31–41)**, built docs-first per user's choice. **Track A (structure one service, deepens 25):** 31 DDD-tactical (entities/value-objects/aggregates+invariants, domain events, repositories, framework-free domain, anti-corruption), 32 Hexagonal/Ports-&-Adapters (driving vs driven ports, in-memory adapters, composition root, contrast w/ 25's layering), 33 DI & Wiring (composition root, manual DI vs google/wire vs uber/fx, kill service-locator/globals). **Track B (coordinate services, deepens 27):** 34 Event-Driven & the **Transactional Outbox** (at-least-once + idempotent consumers, dual-write problem, relay w/ FOR UPDATE SKIP LOCKED, event versioning), 35 Sagas (why-not-2PC, orchestration vs choreography, compensations, idempotency keys, semantic locks — solves 27's distributed-txn trap), 36 Resilience (timeout/retry+jitter/**circuit breaker**/bulkhead/rate-limit/load-shed/graceful-degradation, the composed stack), 37 CQRS & Event Sourcing (CQRS-lite default, event-sourced aggregate Apply/replay/snapshots/projections, upcasting, when-NOT-to). **Track C (production cross-cutting):** 38 Caching (cache-aside/through/behind, TTL+invalidation, **stampede+singleflight**, local/distributed/two-tier, negative caching), 39 Observability: **Distributed Tracing** (three pillars, OpenTelemetry spans, context propagation HTTP/gRPC, trace_id↔logs↔metrics, sampling — adds the 3rd pillar to 27's logs+metrics), 40 Testing Architecture (test pyramid, **fakes>mocks**, testcontainers-go, determinism/clocks/-race, contract testing), 41 API Design & Evolution (backward-compat & versioning, **cursor pagination**, idempotency keys, RFC 9457 problem+json, conventions, OpenAPI). Every lesson uses the standard Goals/Concepts/Exercises/Best-Practices/Checklist/Resources shape and cross-links heavily (to 15/22/25/27/31–41). README gained a Part 9 section (3 tracks); this table has rows 31–41. Scope decisions this session: blended GoF+idiomatic (28–30) done earlier; user then asked for architecture lessons → chose to build **all three tracks**. **Examples:** user asked for graded easy→hard examples, "~50 total this pass", docs-first — so next I build a ~50-example library across the **design-patterns lessons 28–30** (the self-contained runnable set), verified. Architecture (31–41) examples are a later pass. Note: architecture-lesson snippets are illustrative (many touch Kafka/Redis/OTel/testcontainers) — NOT all compile-checked, unlike the pure-Go pattern snippets. Next: build the 28–30 examples libraries.
- 2026-07-07 — ✅ **Design Patterns examples libraries (28–30), 50 graded examples total, all run-verified.** User asked for easy→hard examples, "~50 total this pass", docs-first — so this pass covers the three pattern lessons (self-contained runnable Go); architecture (31–41) examples are a later pass. Each library follows the lesson-27 shape (README + 1-easy/2-medium/3-hard + PROGRESS), stdlib-only, each example a standalone `package main` the user retypes; **Output blocks are real stdout** (authored each as its own runnable file under a scratch module, `gofmt -l`/`go vet` clean, ran every one). **28-patterns-creational/** (17: zero-value, NewT validation, defaults, single/multi/failable options, fluent builder, builder-with-Build-error, factory switch, init() registry, abstract-factory kit, sync.Once singleton [concurrent, 100 goroutines→init once], sync.Pool, deep Clone, **Rob-Pike self-referential undo option**, generic options, capstone HTTP-client). **29-patterns-structural/** (17: struct embedding, interface embedding, embed-interface decorator, HandlerFunc adapter, struct adapter, embedding+shadowing, function decorator, middleware Chain [enter/exit order], stacking interface decorators, facade, caching proxy [counts underlying calls], protection proxy, composite file-tree, bridge, flyweight interning, io.Writer adapter, capstone layered store). **30-patterns-behavioral/** (16: strategy-as-func, slices.SortFunc strategy, command closure queue, observer callbacks, transition-table state machine, chain-of-responsibility, interface strategy, command Do/Undo+history, **Template-Method embedding trap** [Derived.Run→"base step"], template-method fix via injection, type-switch visitor, **range-over-func iter.Seq + early break**, iter.Seq2, channel pub/sub observer, state-as-object, capstone undoable stack calculator [strategy+command+iterator]). Examples 12/13/16 of lesson 30 need Go 1.23+ (noted in their README). README Part 8 now links each examples dir; this table's 28–30 rows updated. Scratch files live in the session scratchpad (not the repo). Next: user works the examples, or asks for architecture (31–41) example libraries.
- 2026-07-07 — 🚧 **Architecture examples (31–41) — started; 31 done, 32–41 pending.** Attempted all 11 in one parallel workflow (11 build + 11 verify agents) but it **hit the session usage limit** (resets ~5:10am Asia/Saigon) — all build agents errored, nothing landed, repo stayed clean. Fell back to building **directly in the main loop, one lesson at a time**. ✅ **`examples/31-ddd-tactical/`** done: 15 graded examples (easy 1–5 value objects/entities/constructor/rich-vs-anemic; medium 6–10 aggregate-invariant/domain-events/reference-by-ID/repository-iface+in-mem/domain-service; hard 11–15 factory/derived-total/**anti-corruption layer**/optimistic-version-lock/**capstone order domain** [value object+aggregate+event+repo+app flow]). Stdlib-only, deterministic, every Output real stdout (`gofmt -l`/`vet` clean, all 15 ran). README Part 9 + table row 31 link the examples. **▶ Resume: build 32–41 example libraries the same way (one lesson at a time to avoid re-tripping the parallel cap), ~15 each.** Approach for the infra-heavy ones: stdlib-only in-memory stand-ins (in-mem broker for 34/35, in-mem cache for 38, a mini span-tree tracer for 39, fakes/spies/injected-clock for 40, net/http+httptest for 32/41) so every example stays `go run .`-able offline.
- 2026-07-07 — ✅ **Track A architecture examples COMPLETE (31, 32, 33)** — built directly in the main loop, one lesson at a time (after the parallel workflow tripped the session cap). Each: 15 graded examples easy→hard, stdlib-only, deterministic, real captured stdout (`gofmt -l`/`vet` clean, all ran). **32-hexagonal-ports-adapters/** (driven/driving ports, in-mem adapters, swap-without-touching-core, composition root; medium: HTTP+CLI driving adapters via httptest, in-mem fake for tests, small consumer-defined ports, dependency-inversion; hard: full mini hexagon, decorated driven adapter, **error translation at boundary**, failable gateway fake, **capstone HTTP→use-case→repo+events end-to-end**). **33-dependency-injection/** (constructor injection, testable ctor, interface swap, composition root, multi-dep; medium: provider functions, provider set, **hand-written injector = what google/wire generates**, service-locator anti-pattern, package-global anti-pattern; hard: **fx-style lifecycle hooks** [start order / reverse stop], config-once-at-root, interface segregation, decorator wiring, **capstone full composition root** with graceful shutdown). wire/fx shown as stdlib stand-ins (no external deps). README Part 9 + table rows 32/33 link examples. **▶ Resume: Track B (34 event-driven+outbox, 35 sagas, 36 resilience, 37 CQRS/ES) then Track C (38–41), same one-lesson-at-a-time approach.**
- 2026-07-07 — ✅ **Track B examples HALF done (34, 35)** — same one-lesson-at-a-time main-loop approach, 15 verified stdlib examples each. **34-event-driven-outbox/** (in-mem bus, commands-vs-events, fan-out, typed events, notification-vs-state-transfer; at-least-once redelivery, idempotent consumer [dedup + naturally-idempotent op], **dual-write bug**, ack-ordering; **transactional outbox**, relay-is-at-least-once [duplicate], partition ordering, DLQ, **capstone full outbox flow** save+event→relay→idempotent projection). **35-sagas-distributed-transactions/** (linear saga, step+compensation, orchestration-with-reverse-compensation, semantic-undo, terminal state; reverse-order-compensation log, idempotent steps + compensations, persist+resume-from-checkpoint, choreography event chain; **semantic lock**, captured-compensation-data, missing-isolation dirty-read, orchestration-vs-choreography, **capstone order saga** reserve→charge→ship fails→full idempotent compensation restores state). README Part 9 + table rows 34/35 link examples. **▶ Resume: 36 resilience (timeout/retry+backoff/circuit-breaker/bulkhead/rate-limit — use fixed seed or deterministic backoff), 37 CQRS/ES, then Track C 38–41.**
- 2026-07-07 — ✅ **Track B COMPLETE (34, 35, 36, 37)** — 36 + 37 built this pass, 15 verified stdlib examples each. **36-resilience-patterns/** (context timeout via past deadline, retryable classification, retry loop, exponential backoff [printed not slept], **seeded jitter** [reproducible]; circuit-breaker state machine, breaker fail-fast, **bulkhead** channel-semaphore, token-bucket rate limiter, load shedding; retry-only-idempotent, retry+breaker composition, graceful-degradation fallback, retry budget, **capstone full stack** rate-limit→bulkhead→breaker→retry→fallback). **37-cqrs-event-sourcing/** (CQRS write/read split, CQRS-lite one-store view, command handler, query DTO, eventual-consistency lag; **Apply fold**, command→events decide, replay/Load+version, optimistic-concurrency append, multi-aggregate store; **snapshots** [= full replay], projection, rebuildable projection, **upcasting** v1→v2, **capstone ES+CQRS** commands→append-only store→replay write side + projection read model). All deterministic (36 avoids real sleeps; seeds rand). README Part 9 + table rows 36/37 link examples. **Architecture examples now: Track A (31-33) ✅, Track B (34-37) ✅ = 105 examples. ▶ Resume: Track C — 38 caching (cache-aside/TTL/stampede-singleflight-lite/LRU), 39 tracing (mini in-mem span-tree tracer + context propagation), 40 testing (fakes/spies/mocks/injected-clock/golden-file via os.TempDir), 41 API design (cursor pagination/idempotency-key/problem+json/ETag via net/http+httptest).**
- 2026-07-07 — ✅ **Track C examples HALF done (38, 39)** — 15 verified stdlib examples each. **38-caching-patterns/** (cache-aside, hit/miss, invalidate-on-write, **TTL via injectable clock**, negative caching; write-through, write-behind [crash loses], **LRU via container/list**, cache **stampede** [100 loads], **mini singleflight** [1 load, deterministic under -race via arrival barrier]; two-tier L1/L2, jittered TTL [seeded], stale-set race, per-tenant key namespacing, **capstone cache-aside+TTL+metrics**). **39-observability-tracing/** (**mini in-mem tracer** stand-in for OTel — counter IDs, injectable clock: span, trace/span ids, parent/child via context, attributes, status/error; **span tree**, in-process ctx propagation, **traceparent inject/extract across a hop**, trace_id-in-logs correlation, three pillars; head sampling, span timing, span links, low-cardinality route-template attribute, **capstone mini distributed trace** gateway→orders one trace_id + tree). README Part 9 + table rows 38/39 link examples. **Architecture examples: 135 total. ▶ Resume: Track C tail — 40 testing (test pyramid, fakes vs mocks/spies, injected clock, golden-file via os.MkdirTemp/WriteFile, table-driven), 41 API design (cursor pagination base64 token, idempotency-key middleware, RFC 9457 problem+json, ETag/304, versioning — all net/http+httptest). Then the whole architecture example set (31-41) is done.**
- 2026-07-07 — ✅✅ **ENTIRE ARCHITECTURE EXAMPLE SET COMPLETE (31–41) — Track C finished (40, 41).** **40-testing-architecture/** (test pyramid, hand-written fake, stub, spy, mock; fake-outcome-vs-mock-interaction, table-driven, **injected clock**, httptest handler, **golden-file via os.MkdirTemp**; test-double taxonomy, **no-sleep determinism** [WaitGroup+chan], contract/field-diff check, reusable fake, **capstone use-case unit test** fake repo+spy mailer+fixed clock). **41-api-design-evolution/** (status codes, verb idempotency, backward-compat add-field, breaking-change field diff, **URI versioning side-by-side** via mux; offset-pagination-drift, **cursor pagination** [opaque base64 token], cap-limit+whitelist-sort, **idempotency-key middleware** [replay], **RFC 9457 problem+json**; **ETag/304**, rate-limit 429+Retry-After headers, money-minor-units/UTC/enum conventions, Deprecation/Sunset, **capstone versioned resource** GET cursor + POST idempotency + problem+json via httptest). All 15+15 stdlib-only (net/http+httptest), deterministic. README Part 9 + table rows 40/41 link examples. **🎉 FULL Part 9 examples: 31–41 = 11 libraries × 15 = 165 graded examples, every one gofmt/vet clean and run-verified in a scratch module.** Combined with 28–30 (50), the whole design-patterns + architecture example corpus is **215 verified examples**. Scratch files in session scratchpad, not the repo. Next: user works the examples, or a new topic.
